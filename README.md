# C++ Library Management System

A simplified library management system demonstrating encapsulation,
inheritance, abstraction, and composition/association in C++.

## Structure
- include/  — header files (.h)
- src/      — source files (.cpp)

## Build
g++ -std=c++17 -Iinclude src/*.cpp -o lms_demo

## Run
./lms_demo
